# 2026-05-16 — Saturday W20

## ~evening — DDD article: drafted, reviewed, parked

- Decision: wrote "Domain-Driven Design: The Fancy Name for Principle Thinking" for boringsystems writing lane (EN + FR). Technical voice target. Anchored in Enakl ride-hailing experience (booking/rider/wallet/driver domains, cancellation flow).
- State: two full drafts. Daniel reviewed twice. First pass: directional grade ~B+. Compressed middle section, anchored production incident (BookingConfirmed event awaiting wallet check), grade rose. Cross-ref-check + french-audit clean. Committed to `omraneah/domain-driven-design`, PR #49 opened.
- Conflict: comparative review against existing boringsystems articles flagged 8 contested / false claims. Most-load-bearing:
  - EventEmitter behavior: Node re-throws unhandled listener errors by default; the silent `undefined` is wrong without custom handler.
  - Historical framing inverted (Evans 2003 predates cheap cloud migrations — the "constraint gone" argument doesn't hold chronologically).
  - "Analytics replica" mis-labeled as CQRS.
  - Outbox: "race condition" should be "failure mode."
- Decision: park. "I'm not writing this article. Clearly, it has some risky false narratives." Branch deleted, PR closed manually (GitHub MCP write connector failed mid-session — auth error on `update_issue` despite read working).
- Action taken: created Linear BOR-49 in Writing backlog, low priority, with EN article content inlined verbatim as a comment (not file-path reference — Ahmed corrected sharply mid-task). 8 flags + caveats captured in card body.

## ~earlier — npm audit blocker on push

- State: pre-push hook blocked on `devalue 5.6.3-5.8.0` (GHSA-77vg-94rm-hx3p), high severity.
- Decision: `npm audit fix` (non-breaking). Lockfile committed separately as `chore(deps): fix high-severity devalue vulnerability`. Hook returned green.

## State

DDD article parked. Linear BOR-49 holds the durable artifact (EN content + critique). PR #49 + branch to be closed/deleted by Ahmed manually due to GitHub write-connector auth failure. No content shipped to boringsystems main from this session; the cleanup work (npm audit fix) did ship.

The session's load-bearing outcome is negative: caught false claims before publication. The article saga reinforced the comparative-review-against-existing-corpus step as essential for any technical-voice piece making contested claims.

## Observations logged (not surfaced — for weekly consolidation)

| Observation | Why noted |
|---|---|
| GitHub MCP write connector failed mid-session (auth error on `update_issue` despite `list_pull_requests` succeeding). Reads work, writes don't. | Pattern may repeat. If it persists across sessions, connector needs reconnection. First occurrence — log only. |
| Comparative-review-against-existing-corpus step (asking Daniel to compare a new article against shipped ones) caught 8 false claims in DDD piece. | Pattern: any technical-voice article making contested historical / behavioral claims needs this step before publish. Currently informal. |
| Linear-card-self-containment rule got triggered loudly mid-session: card initially referenced file paths instead of inlining content. Ahmed's correction was sharp. | The rule is documented in `feedback_linear_cards_self_contained.md`. The trigger condition ("source-of-truth branch will be deleted") wasn't pattern-matched fast enough. |
| Article-write sessions that end in "park" should still produce a Linear card with full content inlined as the durable artifact. | Confirmed via BOR-49 shape. Reinforces card-as-substrate over chat-as-substrate. |
| GitHub MCP write failures leave Ahmed to do 30-second manual ops. Acceptable if rare. | Watch frequency. |
